# Core dependencies
#
# For a fully pinned, reproducible install (exact versions of every
# transitive dependency), see requirements-lock.txt — generate/refresh
# it with: pip install pip-tools && pip-compile requirements.txt
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
stable-baselines3[extra]>=2.0.0
sb3-contrib>=2.0.0
streamlit>=1.30.0
requests>=2.31.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.22.0
yfinance>=0.2.36           # for AI model training data download

# Credential encryption (strongly recommended)
cryptography>=41.0.0

# Broker APIs — Fyers is installed by default.
# To switch brokers, run:  python scripts/install_broker_sdk.py
fyers-apiv3                    # Fyers Securities (default, active)
# kiteconnect>=5.0.1           # Zerodha Kite Connect  → run installer script
# smartapi-python>=1.3.9       # Angel One SmartAPI    → run installer script
# websocket-client>=1.6.0      # WebSocket client (required by Angel One SDK)

# TOTP 2FA. Previously commented out as "required by Angel One", but
# scripts/auth/auto_login_fyers.py imports it unconditionally for the Fyers
# TOTP step — i.e. the zero-touch orchestrator's very first action. A fresh
# install from this file would have had the daily auto-login fail on an
# ImportError before the market opened.
pyotp>=2.9.0

# ---------------------------------------------------------------------------
# Previously undeclared. All were present locally only as transitive
# dependencies of other packages (Pillow via matplotlib, aiohttp via the
# broker SDKs), so the system worked here and would have broken on a clean
# install, in Docker, or in CI the moment an upstream dropped them.
# Found by an AST scan of every import against this file (2026-09-09).
# ---------------------------------------------------------------------------
Pillow>=10.0.0                 # shared/alerts/image_generator.py — EOD card render
aiohttp>=3.9.0                 # shared/lot_size_updater.py — async symbol-master fetch

# Market sentiment (shared/sentiment.py). Feeds main.py's live sentiment
# circuit breaker, so a missing import here silently disables a risk control.
feedparser>=6.0.0              # RSS ingestion
vaderSentiment>=3.3.2          # sentiment scoring
deep-translator>=1.11.0        # Telugu headline translation

# Deep RL (drl/). Optional in practice — the live engine runs without them —
# but drl/trading_env.py and drl/marl/*.py import them at module scope.
gymnasium>=0.29.0
torch>=2.0.0

# Plotting
matplotlib>=3.8.0
plotly>=5.0.0

# Excel report output (audit_script.py's Strategy_Audit_Report.xlsx)
openpyxl>=3.1.0

# Async & websockets
websockets>=12.0

# Timezone — used for IST market-hours detection in api_bridge.py
pytz>=2024.1

# Logging
loguru>=0.7.0

# Process management — singleton-instance guard for main.py/api_bridge.py
psutil>=5.9.0
filelock>=3.32.0           # atomic cross-process lock backing the guard above
